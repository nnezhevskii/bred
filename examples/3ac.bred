fun main() {
    // Иммутабельный массив (читать можно, писать нельзя)

    val arr: Int[3] = [1, 2, "str", true] // ok for parsing
    arr[0] = 5 // ОШИБКА: массив константный

    // Мутабельный массив (читать можно, писать в элементы можно)
    //val mutArr: mut Int[3] = [1, 2, 3]
    // или так: val mutArr: [var Int; 3] = [1, 2, 3]

    //mutArr[0] = 5 // РАБОТАЕТ!
}