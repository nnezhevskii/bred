
// only one is possible
typeclass Printable<A> {
    fun toPrettyPrinter(a: A): String
}

instance Printable<Int> {
    fun toPrettyPrinter(a: Int): String {
        return intToString(a)
    }
}

fun prettyPrint<A: Printable>(a: A) {
    println(toPrettyPrinter(a))
}

fun main(): Unit {
    val a: Int = 0
    prettyPrint(a) // << ok

    prettyPrint("Hello World") << not ok
}

TODO:
1. Парсер << Здесь распарсили кучу мусора. Готово.
2. Синтаксический анализ << Здесь мы проверяем, что тип существует, хотя, наверное, не должны. Возникает проблема с шаблонами.
3. Семантический анализ, тайпчекинг и прочее << вот здесь как будто код уже менять не надо.
4. Компиляция в 3АК
5. Компиляция в бекенд

Нужно менять структуру компилятора.
1. Парсер. Парсер кучу мусора. Уже сделано.
2. Синтаксический анализ.
   (1) строим ASD и доказываем, что программа _выглядит_ корректно
   (2) собираем информацию о том, что у нас есть: функции, глобальные переменные, типы, шаблонные функции, тайпклассы и т.д. Короче говоря - строим контекст
2.5 Кодогенерация.
    Шаг 1: Проход по всему дереву. Заполняем таблицу типов, видим выражение:
      - Если есть тип и он известен, то ничего не делаем
      - Если есть вызов шаблонной функции, то генерируем код, где, условно A меняем на "Int"
    Шаг 2: тут хз
Шаги 3 - тайпчекинг, проверка переменных, и т.д. и т.п.
4, 5 - не меняются

[Шаг 1: Парсер] -> Сырое AST (с мусором)
       |
[Шаг 2: Синтаксис] -> Доказываем корректность + собираем Глобальный Контекст
       |
[Шаг 2.5: Клонатор] -> Находим вызовы в main() -> Крутим Очередь (Worklist)
       |               Генерируем копии функций (prettyPrint__Int, и т.д.)
       |               Выкидываем нахрен исходные шаблоны из дерева.
       |
[Шаг 3: Тайпчекер] -> Строго проверяет финальное, чистое от шаблонов дерево
       |
[Шаг 4, 5: Бекенд] -> Генерирует 3АК и Си-код без единого изменения