fun isPositive(x: Int): String {
    if (x > 0) {
        return "positive"
    }
    else {
        if (x < 0) {
            return "negative"
        } else {
            return "zero"
        }

    }
}

fun main(): Unit {
    var x: Int = readInt()
    println("number: " + isPositive(x))
}